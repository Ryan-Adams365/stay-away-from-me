import 'package:flutter/material.dart';

class Prompt extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is the prompt widget'),
    );
  }
}